---
title: Modelos de datos
sidebar_position: 1
---
# Modelos de datos

Todas as clases de datos do paquete `models` están anotadas con `@Serializable` de `kotlinx.serialization`. Son inmutables por defecto e úsanse tanto para deserializar as respostas de Supabase como para serializar os corpos das solicitudes de inserción e actualización.

---

## Usuario

**Fichero:** `models/Usuario.kt` 
**Tabla Supabase:** `usuarios`

```kotlin
@Serializable
data class Usuario(
    @SerialName("id")
    val id: Int = 0,
    val nombre: String,
    val dni: String,
    val contraseniaHash: String,
    val admin: Int,
    val premium: Boolean,
    val email: String? = null,
    val telefono: String? = null,
    val foto: String? = null,
    @SerialName("fecha_ini_prem")
    val fechaInicio: String? = null,
    @SerialName("fecha_fin_prem")
    val fechaFin: String? = null
)
```

| Campo | Tipo | anulable | Descrición | 
|---|---|---|---| 
| `id` | `Int` | Non | PK autoincremental (por defecto 0 ao inserir) | 
| `nombre` | `String` | Non | Nome completo | 
| `dni` | `String` | Non | DNI usado como credencial de inicio de sesión | 
| `contraseniaHash` | `String` | Non | Contrasinal (texto simple nesta versión) | 
| `admin` | `Int` | Non | 0 = usuario, 1 = administrador | 
| `premium` | `Boolean` | Non | Estado da subscrición | 
| `email` | `String?` | Si | Correo electrónico | 
| `telefono` | `String?` | Si | Teléfono de contacto | 
| `foto` | `String?` | Si | Imaxe de perfil en Base64 | 
| `fechaInicio` | `String?` | Si | Data de inicio da subscrición Premium (`yyyy-MM-dd`) | 
| `fechaFin` | `String?` | Si | Data de finalización da subscrición Premium (`yyyy-MM-dd`) |

Os campos `fechaInicio` e `fechaFin` lense en `ProfileViewModel` para calcular os días restantes da subscrición Premium e mostralos en `ProfileScreen`.

---

##Datos da sesión

**Fichero:** `models/SesionData.kt`

```kotlin
data class SesionData(
    val userId: Int,
    val userName: String,
    val isAdmin: Boolean,
    val isPremium: Boolean,
    val token: String = ""
)
```

Modelo utilizado por `SecureSessionManager` para representar a sesión persistente do usuario. Non está anotado con `@Serializable` porque non está serializado en JSON; gárdase campo por campo en `SharedPreferences`.

---

## Máquina

**Fichero:** `models/Maquina.kt` 
**Tabla Supabase:** `maquinas`

```kotlin
@Serializable
data class Maquina(
    val id: Int,
    val nombre: String,
    val tipo: String? = null,
    val estado: Int = 0,
    val descripcion: String? = null,
    val operativa: Boolean = true,
    val foto: String? = null,
    val mantenimiento_desde: String? = null,
    val mantenimiento_hasta: String? = null
)
```

| Campo | Tipo | anulable | Descrición | 
|---|---|---|---| 
| `id` | `Int` | Non | PK autoincremental | 
| `nombre` | `String` | Non | Nome da máquina | 
| `tipo` | `String?` | Si | Categoría de exercicios | 
| `estado` | `Int` | Non | Campo de estado xenérico (valor predeterminado 0) | 
| `descripcion` | `String?` | Si | Descrición detallada | 
| `operativa` | `Boolean` | Non | `true` = dispoñible para reservas | 
| `foto` | `String?` | Si | Imaxe da máquina en Base64 | 
| `mantenimiento_desde` | `String?` | Si | Inicio do período de mantemento (`yyyy-MM-dd HH:mm`) | 
| `mantenimiento_hasta` | `String?` | Si | Fin do período de mantemento (`yyyy-MM-dd HH:mm`) |

---

## Reserva

**Fichero:** `models/Reserva.kt` 
**Tabla Supabase:** `reservas`

```kotlin
@Serializable
data class Reserva(
    val id: Int? = null,
    @SerialName("id_usuario") val idUsuario: Int,
    @SerialName("id_maquina") val idMaquina: Int,
    val fecha: String,
    @SerialName("hora_inicio") val horaInicio: String,
    @SerialName("hora_fin") val horaFin: String,
    val estado: Boolean = true
)
```

| Campo | Tipo | anulable | Descrición | 
|---|---|---|---| 
| `id` | `Int?` | Si | PK (nulo na inserción, asignado por Supabase) | 
| `idUsuario` | `Int` | Non | FK → usuarios.id | 
| `idMaquina` | `Int` | Non | FK → máquinas.id | 
| `fecha` | `String` | Non | Data de reserva (`yyyy-MM-dd`) | 
| `horaInicio` | `String` | Non | Hora de inicio (`HH:mm`) | 
| `horaFin` | `String` | Non | Hora de finalización (`HH:mm`). Máximo 60 minutos despois de `horaInicio` | 
| `estado` | `Boolean` | Non | `true` = activo, `false` = cancelado |

## ReservaConDetalles

Clase de datos ampliada para consultas con relacións incrustadas (JOIN implícito PostgREST).

```kotlin
@Serializable
data class ReservaConDetalles(
    val id: Int,
    val fecha: String,
    @SerialName("hora_inicio") val horaInicio: String,
    @SerialName("hora_fin") val horaFin: String,
    val estado: Boolean,
    @SerialName("usuarios") val usuario: Usuario? = null,
    @SerialName("maquinas") val maquina: Maquina? = null
)
```

Obtense coa columna `"*, usuarios(*), maquinas(*)"`.

---

## Platón

**Fichero:** `models/Plato.kt` 
**Tabla Supabase:** `platos`

```kotlin
@Serializable
data class Plato(
    val id: Int? = null,
    @SerialName("id_usuario") val idUsuario: Int? = null,
    val nombre: String,
    val descripcion: String? = null,
    val calorias: Double? = null,
    @SerialName("imagen_url") val imagenUrl: String? = null,
    @SerialName("fecha_subida") val fechaSubida: String? = null,
    val visibilidad: Boolean = true,
    val tiempo: String? = null,
    val aceptado: Boolean = false
)
```

| Campo | Tipo | anulable | Descrición | 
|---|---|---|---| 
| `id` | `Int?` | Si | PK (nulo na inserción) | 
| `idUsuario` | `Int?` | Si | FK → users.id (nulo para as antenas do sistema) | 
| `nombre` | `String` | Non | Nome do prato | 
| `descripcion` | `String?` | Si | Receita ou descrición | 
| `calorias` | `Double?` | Si | Calorías por porción | 
| `imagenUrl` | `String?` | Si | Cadea de imaxe Base64 | 
| `fechaSubida` | `String?` | Si | Marca de tempo de creación | 
| `visibilidad` | `Boolean` | Non | `true` = visible no catálogo | 
| `tiempo` | `String?` | Si | Tempo de preparación en texto libre (p. ex. `"30 minutos"`) | 
| `aceptado` | `Boolean` | Non | `false` = pendente de moderación polo administrador |

---

## PesoUsuario

**Fichero:** `models/PesoUsuario.kt` 
**Tabla Supabase:** `peso_usuario`

```kotlin
@Serializable
data class PesoUsuario(
    val id: Int? = null,
    @SerialName("id_usuario") val idUsuario: Int,
    val peso: Double,
    val fecha: String
)
```

| Campo | Tipo | anulable | Descrición | 
|---|---|---|---| 
| `id` | `Int?` | Si | PK (nulo na inserción) | 
| `idUsuario` | `Int` | Non | FK → usuarios.id | 
| `peso` | `Double` | Non | Peso en quilogramos | 
| `fecha` | `String` | Non | Data de rexistro (`yyyy-MM-dd`) |

---

## Pagar

**Fichero:** `models/Pago.kt` 
**Tabla Supabase:** `pagos`

```kotlin
@Serializable
data class Pago(
    val id: Int? = null,
    @SerialName("id_usuario") val idUsuario: Int,
    val monto: Double,
    @SerialName("fecha_pago") val fechaPago: String,
    val tipo: String,
    @SerialName("metodo_pago") val metodoPago: String
)
```

| Campo | Tipo | anulable | Descrición | 
|---|---|---|---| 
| `id` | `Int?` | Si | PK (nulo na inserción) | 
| `idUsuario` | `Int` | Non | FK → usuarios.id | 
| `monto` | `Double` | Non | Importe en euros | 
| `fechaPago` | `String` | Non | Data de pago (`yyyy-MM-dd`) | 
| `tipo` | `String` | Non | Modalidade: Mensual, Semestral, Anual | 
| `metodoPago` | `String` | Non | Método: Tarxeta, Bizum |